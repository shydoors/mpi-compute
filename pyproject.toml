[project]
name = "mpi-compute"
version = "0.1.0"
description = "kmeans的脚本"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.2.6",
    "pandas>=2.3.3",
    "pip>=26.1.2",
]
