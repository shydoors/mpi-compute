{
    "_description": "K-Means 运行配置 — 修改此文件后直接运行程序即可，无需重新编译",
    "data_path": "data/b.dat",
    "backend": {
        "value": "cuda",
        "description": "seq (串行) / omp (OpenMP多核) / cuda (GPU加速)"
    },
    "auto_k": false,
    "fixed_k": 500,
    "max_iterations": 30,
    "threshold": 1e-4,
    "streaming": false,
    "checkpoint_path": "",
    "resume": false,
    "output": {
        "render_max_points": 100000,
        "description": "渲染采样上限，当总点数超过此值时，自动均匀采样后再导出 CSV"
    }
}
